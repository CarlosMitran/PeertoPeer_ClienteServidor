Carlos Mitran y Helena González
100472086@alumnos.uc3m.es 100451026@alumnos.uc3m.es

IMPORTANTE: LOS PAQUETES SPYNE Y ZEEP DEBEN ESTAR INSTALADOS CON PIP INSTALL

Antes de la ejecución, se debe hacer:
export LD_LIBRARY_PATH=./:$LD_LIBRARY_PATH
export IP_TUPLAS=localhost

Tras esto, se abrirán cuatro terminales, para ver todos los resultados, y ejecutaremos:
./servidor -p <puerto>
python3 ./client.py -s ip -p puerto
python3 ./ServicioFecha.py
./rpc_servidor

El archivo testfile es un archivo de prueba para probar get_file fácilmente. El resto de archivos son necesarios para
la correcta ejecución del programa. Published_content guarda el contenido publicado. Connected_usr los usuarios conectados
y reg_users los usuarios registrados.
